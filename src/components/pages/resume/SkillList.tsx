import {FC} from 'react';
import styled from 'styled-components';

const SkillContainer = styled.div`
  height: 25vh;
  background-color: green;
`;

const SkillList: FC = () => <SkillContainer></SkillContainer>;

export default SkillList;